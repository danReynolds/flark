[package]
name = "flark_comrak_bridge"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
comrak = "0.50"
entities = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
