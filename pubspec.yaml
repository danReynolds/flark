name: flark_workspace
description: Repository qualification harness for the flark and flark_flutter packages.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.10.4

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
