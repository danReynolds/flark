{
  "name": "flark-docs",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "dev": "npm run build:playground && astro dev --host 127.0.0.1 --base /",
    "dev:site": "astro dev --host 127.0.0.1 --base /",
    "build:playground": "node scripts/build-playground.mjs",
    "build": "npm run build:playground && astro build",
    "preview": "astro preview --host 127.0.0.1"
  },
  "dependencies": {
    "@astrojs/starlight": "0.40.0",
    "astro": "6.4.8"
  }
}
