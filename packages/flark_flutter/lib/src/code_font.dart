/// Bundled code face, including Flutter's package namespace.
///
/// A CSS generic such as `monospace` is not a registered Flutter web font.
const flarkCodeFontFamily = 'packages/flark_flutter/FlarkMono';
