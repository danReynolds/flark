[package]
name = "flark-abi"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib", "rlib"]

[features]
default = []
declarations = []

[dependencies]
flark-runtime = { path = "../flark_runtime" }

[dev-dependencies]
serde_json = "1.0"
