name: flark_peer_benchmark
description: Isolated peer-editor perf comparison (flutter_quill). NOT part of the flark package.
publish_to: none
version: 0.0.0
environment:
  sdk: ^3.10.0

dependencies:
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  flutter_quill: ^11.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
