[package]
name = "flark-comrak-derived-core-probe"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]

[dependencies]
comrak = { path = "../comrak_inline_fragment_gate/vendor/comrak", default-features = false }
im = "15.1.0"

[dev-dependencies]
serde_json = "1.0"
