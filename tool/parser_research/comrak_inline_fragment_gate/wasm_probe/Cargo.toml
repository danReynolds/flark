[package]
name = "flark-comrak-inline-fragment-wasm-probe"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
comrak = { path = "../vendor/comrak", default-features = false }

