[package]
name = "flark-lazy-inline-fact-cache-wasm-probe"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
flark-lazy-inline-fact-cache-gate = { path = ".." }

[profile.release]
opt-level = 3
lto = "thin"
panic = "abort"

