[package]
name = "flark-md4c-derived-block-core"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
comrak = { path = "../comrak_inline_fragment_gate/vendor/comrak", default-features = false }
im = "15.1.0"

[dev-dependencies]
serde_json = "1.0"

[lints.rust]
unsafe_code = "forbid"

[profile.release]
debug = true
