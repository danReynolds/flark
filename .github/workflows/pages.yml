name: GitHub Pages (v4 web pending)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  web-support-status:
    runs-on: ubuntu-latest
    steps:
      - name: Record the active support boundary
        run: |
          echo "Flark v4 currently has no web backend or web example."
          echo "This workflow intentionally performs no deployment until that product surface is implemented and qualified."
