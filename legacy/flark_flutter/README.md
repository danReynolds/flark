# Archived flark_flutter implementation

This directory preserves v2/v3 source and tests as historical engineering
evidence. It is non-publishable and is not referenced by the active build,
runtime, package, or verification path.

The products are:

- `packages/flark_core`: headless Dart plus the Rust runtime and native hook.
- `packages/flark`: the Flutter editor and read-only surface.
