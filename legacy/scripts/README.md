# Legacy operational scripts

The `v2_v3/` scripts are preserved as historical implementation evidence.
They are not supported entrypoints and are not expected to run from this
location. Active verification starts at `scripts/verify_v4.sh`; release and
archive evidence start at `scripts/verify_v4_release.sh` and
`scripts/verify_v4_publish_archives.sh`.
