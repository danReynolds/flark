# Legacy documentation

The `v2_v3/` tree preserves superseded public guides, design notes, and device
protocols. It describes retired packages and APIs and is not current product
documentation. Active guides live in the repository's top-level `docs/`
directory.
